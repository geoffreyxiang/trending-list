You can generate random foods by calling the function on different properties:
    - name
    - type
    - healthy
    - calories
    - temp

Call the function like this:
` getRandom('name')
  console.log(getRandom('type'))`
